{{- if .Values.opensearchDashboards.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "gochat.fullname" . }}-dashboards
  labels:
    {{- include "gochat.componentLabels" (dict "Context" . "Component" "dashboards") | nindent 4 }}
spec:
  type: {{ .Values.opensearchDashboards.service.type }}
  ports:
    - name: http
      port: {{ .Values.opensearchDashboards.service.port }}
      targetPort: {{ .Values.opensearchDashboards.service.port }}
  selector:
    {{- include "gochat.componentSelectorLabels" (dict "Context" . "Component" "dashboards") | nindent 4 }}
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gochat.fullname" . }}-dashboards
  labels:
    {{- include "gochat.componentLabels" (dict "Context" . "Component" "dashboards") | nindent 4 }}
spec:
  replicas: {{ .Values.opensearchDashboards.replicaCount }}
  selector:
    matchLabels:
      {{- include "gochat.componentSelectorLabels" (dict "Context" . "Component" "dashboards") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "gochat.componentSelectorLabels" (dict "Context" . "Component" "dashboards") | nindent 8 }}
    spec:
      containers:
        - name: dashboards
          image: "{{ .Values.opensearchDashboards.image.repository }}:{{ .Values.opensearchDashboards.image.tag }}"
          imagePullPolicy: {{ .Values.opensearchDashboards.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.opensearchDashboards.service.port }}
              name: http
{{- if .Values.opensearchDashboards.env }}
          env:
{{- range $key, $value := .Values.opensearchDashboards.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
{{- end }}
{{- end }}
          resources:
            {{- toYaml .Values.opensearchDashboards.resources | nindent 12 }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.opensearchDashboards.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.opensearchDashboards.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.opensearchDashboards.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
